server:
  port: 10258

spring:
  application:
    name: admin-service
  datasource:
    url: jdbc:h2:mem:assessment
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
