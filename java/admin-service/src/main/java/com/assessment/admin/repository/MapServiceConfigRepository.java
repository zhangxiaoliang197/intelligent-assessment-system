package com.assessment.admin.repository;

import com.assessment.admin.model.MapServiceConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MapServiceConfigRepository extends JpaRepository<MapServiceConfig, String> {
}
