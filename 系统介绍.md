# 智能评估系统 — 系统介绍

**版本**：V2.0 | **日期**：2026-08-07

---

## 一、产品定位与用户画像

### 1.1 一句话定位

面向作战指挥场景的 **"AI 分析师"**——将非结构化的分析需求，自动转化为结构化的数据库查询、指标计算与综合结论。

### 1.2 核心解决的问题

| 断层 | 传统方式 | 本系统解决 |
|------|----------|-----------|
| **需求→数据** | 人工理解需求 → 找表 → 手写 SQL，依赖 DBA | AI 自动选表 + Text-to-SQL，零 SQL 门槛 |
| **数据→洞察** | 导出 Excel → 人工制图制表 → 写报告，数小时 | 自动执行 + LLM 分析，秒级出结论 |
| **知识→决策** | 专业知识散落在文档/人脑中，无法系统化调用 | RAG 知识库统一管理，对话即检索 |

### 1.3 目标用户

| 角色 | 使用场景 | 核心诉求 |
|------|----------|----------|
| **指挥决策人员** | 快速了解战损/战果/消耗态势 | 自然语言提问，即刻获得数据+图表+结论 |
| **参谋/分析人员** | 构建指标体系、评估作战方案 | 知识库辅助 + LLM 推理 + Skill 编排 |
| **系统管理员** | 配置数据源、管理知识库、维护本体模型 | Web 界面操作，无需命令行 |

---

## 二、系统架构

### 2.1 整体分层

```
                    ┌──────────────────────┐
                    │   Nginx (端口 10086)   │
                    │   Vue 3 前端 SPA       │
                    └──────┬───────────────┘
                           │
     ┌──────────┬──────────┼──────────┬──────────┬──────────┐
     ▼          ▼          ▼          ▼          ▼          ▼
   QA        Know-      Indi-      Onto-      Eval-      Admin
  10253      ledge      cator      logy      uation      (Java)
  FastAPI    10252      10254      10256      10255      10258
  + 多Agent  +Qdrant    +状态机    +分步构建    +CRUD     + JDBC
     │          │          │          │                     │
     └──┬───────┴──────────┴──────────┘      ┌─────────────┘
        │                                    │
        ▼                               JDBC ▼
   Qdrant:6333                        MySQL / Oracle
   向量检索                             / 达梦 / PG /
                                       SQL Server
```

### 2.2 服务职责

| 服务 | 端口 | 技术栈 | 职责 |
|------|------|--------|------|
| **Frontend** | 10086 | Vue 3 + TS + Vite + Element Plus + ECharts + Leaflet | 门户、问答、指标树、评估、地图、知识库、本体图、后台管理 |
| **QA Service** | 10253 | Python FastAPI | 系统大脑：RAG 问答 + 多智能体评估 + Skill 全生命周期 + 定时调度 + 批量执行 |
| **Knowledge Service** | 10252 | Python FastAPI + Qdrant + BGE + BM25 | 文档解析→BGE 嵌入→Qdrant 语义 + BM25 关键词→RRF 融合检索 |
| **Indicator Service** | 10254 | Python FastAPI | 会话状态机：概念问答 / LLM 生成指标体系 / 追问确认 / 查询管线 |
| **Ontology Service** | 10256 | Python FastAPI | 本体 CRUD + 3 步构建流水线 + 图谱 + 数据绑定 + 上下文接口 |
| **Evaluation Service** | 10255 | Python FastAPI | 评估方案 CRUD（轻量，实际执行由 QA 的 Skill 引擎完成） |
| **Admin Service** | 10258 | Java Spring Boot 3 + JPA | 配置中枢：数据源/驱动/数据集/字段标注/指标/LLM/地图/SQL 安全执行 |

### 2.3 服务间通信

| 调用方 | 被调用方 | 端点 | 用途 |
|--------|----------|------|------|
| QA Service | Knowledge Service | `/knowledge/search` | 混合检索知识库 |
| QA Service | Admin Service | `/api/admin/config/llm/active` | LLM 配置 |
| QA Service | Admin Service | `/api/admin/dataset/*` | 数据集/表结构/SQL 执行 |
| Indicator Service | QA Service | `/qa/classify-query`, `/qa/chat/stream` | 查询分类、LLM 流式生成 |
| Indicator Service | QA Service | `/evaluation/indicator-query/stream` | 指标查询管线 |
| Indicator Service | Admin Service | `/api/admin/indicator/list` | 已配置指标 |
| Indicator Service | Admin Service | `/api/admin/database/list` | 可用数据源 |
| Indicator Service | Knowledge Service | `/knowledge/search` | 知识库检索 |
| Indicator Service | Ontology Service | `/ontology/default/context` | 本体上下文 |
| Ontology Service | Admin Service | `/api/admin/config/llm/active` | LLM 配置 |

通信方式：HTTP REST（urllib.request），内网延迟可忽略。

---

## 三、功能模块全景

系统按用户旅程分为两大类、五大模块：

```
         ┌────────────── 辅助系统（建基础） ──────────────┐
         │  知识库 (Knowledge)   本体模型 (Ontology)       │
         │  基础管理 (Admin)       数据源/指标/LLM 配置    │
         └──────────────────────┬──────────────────────────┘
                                │ 为上层提供数据底座
         ┌────────────── 分析工具（用起来） ──────────────┐
         │  智能问答 (QA)    指标分析 (Indicator)         │
         │  评估分析 (Evaluation)  ← Skill 引擎驱动       │
         └──────────────────────┬──────────────────────────┘
                                │
                      门户 (Portal) 统一入口
```

### 3.1 智能问答（QA Service）

**用户心智模型**：像和专家对话一样提问，系统理解意图并给出答案。

```
用户: "上次对抗中红方战损最高的三个单位是哪些？"
  │
  ▼
知识库混合检索（Qdrant 语义 + BM25 关键词）→ RRF 融合排序
  ↓
LLM 综合知识库片段 + 自身知识 → 流式生成回答
  ↓
区分引用来源：[1][2] = 知识库，无标注 = LLM 生成
```

**特性**：
- 双路混合检索：Qdrant 语义（BGE 嵌入） + BM25 关键词 → RRF 融合
- 多轮对话，滑动窗口上下文（默认 5 轮）
- 会话 JSON 持久化 + 原子写入 + .bak 备份
- 多模态输入：图片上传 + 多模态模型检测（gpt-4o/qwen-vl 等）
- 地图标注：LLM 输出【区域标注】【路线标注】→ 前端 Leaflet 渲染
- 知识库检索无结果时仍可依赖 LLM 自身知识
- 地理/坐标类问题自动跳过知识库检索

### 3.2 指标分析（Indicator Service）

**用户心智模型**：输入分析主题，自动生成分层级的指标体系树，确认后可查询实际数据。

**状态机流程**：

```
analyzing（生成指标）→ awaiting_confirmation（追问确认）→ querying（执行查询）→ done
```

**指标来源标注**：

| 来源 | 判定方式 | 可靠性 |
|------|----------|--------|
| **admin-db** | 名称匹配已配置指标（归一化完全相等 > 包含关系 > 2-gram Jaccard ≥ 0.6） | 最高 |
| **knowledge** | 对应知识库检索结果 | 中等 |
| **llm** | 兜底，LLM 自身知识 | 需验证 |

**B 阶段数据联动**：
- 引入本体模型上下文（`ontology/context`），将本体概念和关系注入指标生成 prompt
- 增强指标体系的领域深度和结构化程度

### 3.3 评估分析（Evaluation）— 多智能体 + Skill 引擎

**两种执行路径**：

#### A. 声明式 Skill 引擎

用户创建 Skill（定义步骤序列、数据源映射等），系统自动执行。

**Skill 生命周期**：
```
创建草稿 → 编辑步骤 → 飞行前检查 → 试运行（调试） →
发布 → 执行 → 质量评估 → 执行对比
  │
  ├── 版本管理：发布/回滚/版本历史
  ├── 模板系统：创建模板/实例化
  ├── 分享协作：生成链接/撤销分享/导入导出
  └── 自动化：Cron 定时调度 / 批量执行
```

**Skill 步骤操作类型**：
- `dataset_query` — 数据集关键词匹配 → 自动 SQL 查询
- `database_overview` — 获取全量表结构概览
- `table_catalog` — 列出所有表名
- `table_structure` — 读取指定表 DDL + 标注

**定时调度**：
- Cron 表达式，支持 Asia/Shanghai 时区
- 后台 asyncio 轮询（30 秒间隔），到期自动执行
- 执行记录持久化到 SQLite

#### B. ReAct 多智能体工作流（自动路由）

当用户未选择 Skill 时，系统自动使用 ReAct 工作流：

```
用户提问
  │
  ▼
Orchestrator 意图识别 (LLM)
  │
  ├── data_query → 7 步标准流程
  │    ① 意图识别 → ② 数据源探查 → ③ 检查数据集&指标 →
  │    ④ 智能选表（打分机制） → ⑤ Text-to-SQL →
  │    ⑥ SQL 安全执行 → ⑦ Analyst 分析建议
  │
  ├── combat_effectiveness → 预配置 SQL 模板 + 多维度分析
  │
  ├── air_superiority → 区域参数提取 + SQL 注入 + 红蓝对比
  │
  └── general_analysis → 直接 LLM
```

**智能选表打分机制**：
```
直接提到表名              → +100
表名关键词匹配用户问题     → +20
表名关键词匹配分析计划     → +10
"表 XXX" 模式             → +200
数据集关联的表优先保留
最多 5 张表（数据集额外放宽到 6 张）
```

**SQL 安全校验**（5 层）：
1. 从 markdown 代码块/纯文本提取 SQL
2. 禁止 60+ 高风险函数（PG_SLEEP、LO_IMPORT、UTL_HTTP 等）
3. 禁止 INSERT/UPDATE/DELETE/DROP/ALTER/TRUNCATE
4. 禁止多语句和注释
5. 仅允许 SELECT/WITH 开头，括号匹配检查

### 3.4 知识库（Knowledge Service）

**双路混合检索 RAG**：

```
文档上传（PDF/DOCX/TXT/MD/CSV）
  │
  ▼
文件解析 → 文本分片（chunk=400, overlap=100）
  │
  ├── 语义路：BGE-small-zh-v1.5 嵌入 → Qdrant COSINE 检索
  └── 关键词路：jieba 分词 → BM25Okapi 倒排索引
  │
  ▼
RRF 融合排序（k=60）→ 去重 → 返回 top_k
```

**特性**：
- 支持批量上传和分类/标签管理
- 文档更新自动同步 Qdrant payload
- 全量重建索引（reindex）
- 重新解析已上传文档
- 统计信息（文档数/分片数/向量数/分类数）

### 3.5 本体模型（Ontology Service）

**核心能力**：
- 本体 CRUD + 默认本体设置 + 导入/导出 JSON
- 实体管理 + 关系管理（有向边 + 权重）
- 知识图谱数据（nodes/links）供 ECharts 可视化
- BFS 最短路径查询
- B 阶段数据绑定（实体→字段、关系→指标）
- 上下文接口供 indicator-service 注入 prompt

**3 步构建流水线**：

```
上传文档
  → LLM 推荐元模型（entity_types + relation_types）
  → 用户确认元模型
  → Step 1: LLM 提取概念清单
    （长文档 9000 字/批，500 字重叠，断点续作）
  → 用户确认概念清单
  → Step 2: LLM 构建层次结构
    （同类型 20 个/组，跨组关系补充）
  → 用户确认层次结构
  → Step 3: LLM 最终序列化 → 生成正式本体
```

### 3.6 基础管理（Admin Service）

系统的配置中枢：

- **数据库配置**：连接管理 / 多数据源 / 连接测试（延迟+版本）/ 驱动动态加载
- **数据集管理**：CRUD + 表结构读取（5 种数据库方言）+ 字段标注（业务含义+数据分类）
- **指标管理**：CRUD + 分类 + 数据集关联 + 字段映射（JSON）
- **LLM 配置**：多配置管理 / 活跃切换（互斥）/ 连接测试 / 支持 TLS 配置
- **地图配置**：多地图服务管理 / 活跃切换
- **LLM 学习数据导出**：全量 schema + 字段标注 + 指标定义，供 LLM 学习
- **SQL 安全执行**：5 层校验，查询超时 60s，最多 1000 行

---

## 四、前端架构

### 4.1 技术栈

| 类别 | 技术 |
|------|------|
| 框架 | Vue 3 + Composition API + TypeScript |
| 构建 | Vite 5 |
| UI 库 | Element Plus（自动导入） |
| 路由 | Vue Router 4（History 模式） |
| 状态管理 | Pinia |
| HTTP | Axios + Fetch（流式） |
| CSS | Tailwind CSS |
| 图表 | ECharts（vue-echarts） |
| 地图 | Leaflet + leaflet-draw + gcoord |
| Markdown | marked + DOMPurify |
| 语音 | Web Speech API |

### 4.2 路由表

| 路径 | 页面 | 说明 |
|------|------|------|
| `/` | Portal.vue | 门户首页 |
| `/qa` | QAService.vue | 智能问答 |
| `/indicator` | IndicatorAnalysis.vue | 指标分析 |
| `/evaluation` | SolutionEvaluation.vue | 评估分析 |
| `/evaluation/skills` | SkillsLibrary.vue | Skill 库管理 |
| `/knowledge` | KnowledgeBase.vue | 知识库管理 |
| `/ontology` | OntologyModel.vue | 本体模型管理 |
| `/ontology/:id` | OntologyDetail.vue | 本体详情 |
| `/ontology-build/:jobId` | OntologyBuild.vue | 构建任务详情 |
| `/admin` | AdminSystem.vue | 基础管理 |

### 4.3 核心组件

| 组件 | 功能 |
|------|------|
| **GeoMap.vue** | Leaflet 地图，AI 标注渲染（点/线/面），用户手绘（leaflet-draw），WGS84→GCJ02 坐标转换，GeoWebCache 瓦片叠加，坐标点螺旋偏移，同名同色 |
| **Layout.vue** | 全局布局，侧边栏导航 |
| **FloatingSidebar.vue** | 浮动工具侧边栏 |
| **SkillEditorDialog.vue** | Skill 多步骤编辑器 |
| **SkillOperationsDrawer.vue** | Skill 操作面板（执行/调度/批量/对比） |

### 4.4 Composable 组合函数

| Composable | 功能 |
|------------|------|
| **useAttachmentUpload** | 文档上传（PDF/Word/TXT，≤20MB），进度追踪 |
| **useImageUpload** | 图片上传（PNG/JPG/WebP 等，≤10MB），本地预览 |
| **useSpeechRecognition** | 语音识别（中文普通话，连续识别，实时中间结果） |
| **useMapPrompt** | 地图意图检测，SQL 结果中经纬度列自动配对 |

---

## 五、多智能体与 Skill 引擎深度解析

### 5.1 为什么需要 Skill 引擎？

| 单一 LLM 问答 | Skill 引擎 |
|---------------|-----------|
| 每次从零开始构建上下文 | 预定义步骤序列，上下文可复用 |
| 无法保证执行一致性 | 声明式编排，每次执行路径一致 |
| 无法自动化重复任务 | Cron 定时调度 + 批量执行 |
| 无法对比不同执行结果 | 执行对比功能，横向分析 |
| 不可控的 LLM 行为 | 步骤级别操作约束（dataset_query 等） |

### 5.2 Skill 核心概念

- **Skill**：声明式的评估分析模板，包含名称、描述、分类、触发词、推荐问题、步骤序列、可视化配置、编排模式
- **步骤（Step）**：最小执行单元，定义操作类型、数据集关键词、依赖关系、重试/超时/失败策略
- **飞行前检查（Preflight）**：验证 Skill 步骤所需的数据集是否在指定数据源中存在，输出匹配度报告
- **试运行（Trial）**：单步调试，验证单个步骤的 SQL 生成和执行是否正确
- **批量执行（Batch）**：多个问题并发执行（最大 8 并发），每个问题独立的会话上下文
- **定时调度（Schedule）**：Cron 触发，后台自动执行并记录

### 5.3 智能体组件（`python/qa-service/agents/`）

| 文件 | 职责 |
|------|------|
| `orchestrator.py` | 意图识别、路由选择、分析计划制定 |
| `react_workflow.py` | ReAct 工作流引擎，7 步状态流转 |
| `langgraph_workflow.py` | LangGraph 工作流实现（备用引擎） |
| `text_to_sql.py` | 根据表结构 + 问题生成 SELECT |
| `analyst.py` | 基于查询结果生成分析建议 |
| `combat_effectiveness_agent.py` | 作战效能预配置 SQL 模板 |
| `air_superiority_agent.py` | 制空权分析，提取区域 + SQL 注入 |
| `indicator_query.py` | 模块化指标查询管线节点 |
| `sufficiency.py` | 数据充分性判定 |
| `sql_dialect.py` | SQL 方言适配 |
| `tools.py` / `tools_langchain.py` | 工具层：HTTP 调用 admin-service |
| `state.py` | 共享状态容器 |
| `crewdefs.py` | 智能体角色定义 |
| **Skill 相关** | |
| `skill_catalog.py` | Skill 目录 CRUD + 版本管理 + 搜索推荐 |
| `skill_runner.py` | Skill 工作流执行引擎 |
| `skill_execution_store.py` | SQLite 持久化执行记录 |
| `skill_runtime_service.py` | 批量执行 + Cron 调度器 + 执行对比 |
| `skill_governance.py` | SkillActor 身份模型 + 权限策略 + 导入导出 |
| `skill_ai_service.py` | AI 草案生成（LLM 辅助创建 Skill） |
| `skill_quality_service.py` | 质量评估 + 运维概览 |
| `custom_skill_store.py` | 自定义 Skill 持久化 |

---

## 六、容器化与部署

### 6.1 Docker Compose（8 个容器）

| 容器 | 端口 |
|------|------|
| assessment-frontend | 10086:80 |
| assessment-qdrant | 6333 |
| assessment-knowledge | 10252 |
| assessment-qa | 10253 |
| assessment-indicator | 10254 |
| assessment-evaluation | 10255 |
| assessment-ontology | 10256 |
| assessment-admin | 10258 |

网络：`assessment-net`（bridge 驱动）

### 6.2 Nginx 反向代理

| 路径 | 目标容器 |
|------|----------|
| `/api/qa/*`, `/api/config/*`, `/api/evaluation/*`, `/api/attachment/*`, `/api/image/*`, `/api/model/*` | assessment-qa:10253 |
| `/api/knowledge/*` | assessment-knowledge:10252 |
| `/api/indicator/*` | assessment-indicator:10254 |
| `/api/ontology/*` | assessment-ontology:10256 |
| `/api/admin/*` | assessment-admin:10258 |
| `/tiles`, `/geowebcache` | 外部地图服务:9090 |

### 6.3 离线部署

- 全量 Docker 镜像构建为 `.tar` 文件
- 内网 `docker load` + `docker run` 一键启动

---

## 七、关键技术一览

| 类别 | 技术 |
|------|------|
| 前端框架 | Vue 3 + TypeScript + Vite |
| UI 组件库 | Element Plus + Tailwind CSS |
| 地图 | Leaflet + leaflet-draw + gcoord |
| 图表 | ECharts (vue-echarts) |
| Python Web 框架 | FastAPI + Uvicorn |
| 向量检索 | Qdrant + BGE-small-zh-v1.5 |
| 关键词检索 | jieba + BM25Okapi + RRF 融合 |
| 文档解析 | PyPDF2 + python-docx（含表格） |
| LLM 集成 | OpenAI 兼容 API（deepseek/vllm/openai） |
| 流式响应 | SSE / NDJSON |
| Skill 存储 | SQLite |
| Java 框架 | Spring Boot 3.x + Spring Data JPA |
| 数据库连接 | JDBC + URLClassLoader 动态加载驱动 |
| 支持数据库 | MySQL / PostgreSQL / Oracle / 达梦 V8 / SQL Server |
| 容器化 | Docker + Docker Compose |
| 反向代理 | Nginx（Vue SPA + API 代理） |
| 会话持久化 | JSON 文件（原子写入 + .bak 备份） |

---

## 八、产品设计亮点

| 维度 | 具体体现 |
|------|----------|
| **混合检索** | Qdrant 语义 + BM25 关键词 → RRF 融合，兼顾精度和覆盖 |
| **Skill 引擎** | 声明式步骤编排 + 定时调度 + 批量执行 + 质量评估，评估分析可复用可自动化 |
| **多智能体协同** | Orchestrator 路由 + 专项 Agent + 兜底降级，兼顾专业性与灵活性 |
| **Text-to-SQL 管线** | 智能选表打分 → LLM 生 SQL → 5 层安全校验 → JDBC 执行 → 分析 |
| **指标来源透明** | 代码层标注（admin-db/knowledge/llm），不让 LLM 自标 |
| **本体分步构建** | 3 步流水线 + 人机协同确认 + 长文档批处理 + 断点续作 |
| **地图可视化** | LLM 输出标注格式 → geoParser 解析 → Leaflet 渲染，支持用户手绘叠加 |
| **数据安全** | SQL 执行禁止 60+ 高风险函数，仅允许 SELECT/WITH，查询超时控制 |
| **流式全链路** | SSE/NDJSON 全链路流式，前端实时展示步骤进度和中间结果 |
| **内网可部署** | 离线 `docker load` 一键启动，无外部依赖 |

---

## 九、后续演进方向

1. **语义检索增强**：BM25 → BGE 语义检索完全替代，提升同义词/近义词检索精度
2. **LLM 调用 SDK 化**：替换 `urllib.request`，支持自动重试、流控、连接池
3. **并发写优化**：JSON 文件 → SQLite 迁移，支持会话/知识库并发操作
4. **评估方案模板化**：支持保存和复用评估方案模板，减少重复配置
5. **前端 SSR**：提升首屏加载速度
6. **多模态增强**：支持更多视觉模型，图表识别、文档图片 OCR
