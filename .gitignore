# Dependencies
node_modules/

# Build outputs
dist/
build/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
ENV/
env/
.venv/

# Java
target/
*.class
*.jar
!drivers/*.jar
*.war
*.ear
*.log

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.local
.env.*.local

# Uploads
uploads/
vector_db/

# Qdrant 本地向量库运行时数据（服务自动生成，不入库）
qdrant/

# Models (large files, download separately)
python/knowledge-service/models/

# Database
*.db
*.sqlite
*.sqlite3
*.sqlite3-shm
*.sqlite3-wal

# Docker
docker/images/

# Temporary files
tmp/
temp/
*.tmp

# System files
Thumbs.db
Desktop.ini
llm_config.json
docker-images/
offline-deps/
_build_docker.ps1

# Personal dev docs (don't commit)
CLAUDE.md
CLAUDE.local.md

读我.docx
GeoWebCache-使用指南.md

# Runtime data files (don't commit)
**/data/sessions.json
**/data/history.json
**/data/evaluation_sessions.json
**/data/evaluation_history.json
**/data/*.empty_backup
*.bak

# 本体分步构建任务的运行时状态数据（含文档原文、LLM 结果，服务自动生成，不入库）
python/ontology-service/data/build_jobs/

# 本体数据备份目录（服务自动生成，不入库）
**/data_backup_*/

# 用户本体构建/编辑生成的运行时数据（服务自动生成，不入库）
python/ontology-service/data/user_ontologies/

# Local dev scripts
logs*.bat
sample_data/

# Tile service cache (large tile files)
python/tile-service/tiles/

# 本地启动/停止脚本
start.ps1
stop.ps1

# IDE 本地配置（不提交）
.trae/settings.local.json
.trae/documents/
.trae/rules/personal_rules.md

# 测评体系（本地开发质量工具，不入产品库）
assessment-bench/


python/knowledge-service/data

# 智能提交：根目录一次性调试脚本与本体服务扫描脚本（不入库）
_tmp_*.ps1
python/ontology-service/_scan_manual.py

# 本体 OWL 导出产物（owl_builder 自动生成，可重新生成，不入库）
python/ontology-service/data/ontologies/

# Agent 工作区调试产物（测试 JSON 与附件，不入库）
.agent_context/

# 一次性调试复现工作区（node_modules/截图/脚本，不入库）
.ws9-repro/

